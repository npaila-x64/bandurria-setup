version: "3.9"
services:
  api-gateway:
    image: ${IMAGE_REPO}/asistente_ia-api_gateway
    ports:
      - ${API_GATEWAY_SERVICE_EXTERNAL_PORT}:20000
    networks:
      - assistant_network

networks:
  assistant_network:
      external: true
      name: ${NETWORK_NAME}
